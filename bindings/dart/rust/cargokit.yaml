cargo:
  debug:
    toolchain: stable
  release:
    toolchain: stable

precompiled_binaries:
  # Uri prefix used when downloading precompiled binaries.
  url_prefix: https://github.com/tursodatabase/turso/releases/download/v
  # Public key for verifying downloaded precompiled binaries.
  public_key:
